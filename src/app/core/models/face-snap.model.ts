export class FaceSnap {
  constructor(public id:number, public title:string, public createdDate:Date, public description:string, public snaps:number, public snaped:boolean, public imageUrl:string, public location?:string ) {

  }
}
